/* ============================================================
 *  ⚡ THE ONLY FILE YOU EDIT  ⚡        (lives at: src/config.js)
 * ============================================================ */

/* 1) YOUR TONKEEPER WALLET ADDRESS
 *    Open the Tonkeeper app → tap your wallet → "Receive" →
 *    copy the address → paste it between the quotes below
 *    (delete the PASTE_YOUR... text).
 *
 *    Until you paste a real address, payments are BLOCKED
 *    automatically, so nothing can ever be sent by mistake. */
export const TONKEEPER_WALLET_ADDRESS = 'PASTE_YOUR_TONKEEPER_ADDRESS_HERE';

/* 2) TON PRICE ANCHOR — how many US dollars one TON is worth.
 *    Default $5 → the $5 Launch Pass costs exactly 1 TON.
 *    Change the number if TON's price moves a lot. */
export const USD_PER_TON = 5;

/* 3) APP NAME shown when a wallet connects. */
export const APP_NAME = 'LEXCONV GTM Terminal';
